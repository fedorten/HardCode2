# HardCode2

## Структура проекта

```
├── templates/          # Шаблоны компонентов (редактировать здесь!)
│   ├── header.html    # Общий хэдер для всех страниц
│   ├── footer.html    # Общий футер для всех страниц  
│   ├── modal.html     # Модальное окно формы
│   └── base.html      # Базовый шаблон (не трогать)
├── pages/             # Контент страниц (редактировать здесь!)
│   ├── index.html     # Контент главной страницы
│   ├── about.html     # Контент "О нас"
│   ├── courses.html   # Контент "Курс"
│   ├── prices.html    # Контент "Цены"
│   ├── contact.html   # Контент "Контакты"
│   ├── faq.html       # Контент FAQ
│   ├── offer.html     # Контент оферты
│   └── privacy.html   # Контент политики конфиденциальности
├── *.html             # Сгенерированные файлы (не редактировать!)
├── backup/            # Бэкап оригинальных файлов
├── backup-full-original/ # Полная резервная копия
├── main.css           # Стили
├── script.js          # JavaScript
├── build.js           # Скрипт сборки (не трогать)
├── package.json       # Зависимости и скрипты
└── vercel.json        # Конфигурация Vercel
```

## Основные команды

```bash
# Установка зависимостей
npm install

# Сборка проекта (генерация HTML из шаблонов)
npm run build

# Запуск локального сервера с автоматической сборкой
npm run dev

# Только сборка
npm run start

# Запуск сервера для готовых файлов
npm run serve
```

## Как вносить изменения

### Изменить навигацию/хэдер/футер:
1. Отредактируйте `templates/header.html` или `templates/footer.html`
2. Запустите `npm run build`
3. Изменения появятся на **всех страницах**

### Изменить контент конкретной страницы:
1. Отредактируйте соответствующий файл в `pages/`
2. Запустите `npm run build`
3. Изменения появятся только на этой странице

### Добавить новую страницу:
1. Создайте файл в `pages/имя.html` с контентом
2. Добавьте страницу в массив `pages` в `build.js`
3. Запустите `npm run build`

## Пример изменения хэдера

Изменили навигацию в `templates/header.html`:
```html
<ul class="nav-links">
    <li><a href="courses.html">Курс</a></li>
    <li><a href="prices.html">Цены</a></li>
    <li><a href="about.html">О нас</a></li>
    <li><a href="faq.html">FAQ</a></li> <!-- Новый пункт -->
</ul>
```

После `npm run build` этот пункт появится на **всех 8 страницах**!

## Деплой на Vercel

Проект автоматически соберется на Vercel благодаря `vercel.json`. Просто отправляйте изменения в репозиторий.

## Преимущества

🚀 **SEO-идеально**: Статические HTML файлы для поисковиков  
🔧 **Один источник правды**: Хэдер/футер в одном месте  
⚡ **Быстрая загрузка**: Никакой JavaScript-загрузки контента  
🛠️ **Простота**: Минимальные зависимости, понятная структура  
🔄 **Легкое масштабирование**: Добавляй страницы за 30 секунд  

## Откат изменений

Если что-то пошло не так:
```bash
# Вернуть оригинальные файлы
cp backup/*.html ./
rm -rf templates pages package.json vercel.json build.js

# Полный откат (включая резервную копию)
cp backup-full-original/* ./
rm -rf templates pages package.json vercel.json build.js backup-full-original
```

## Требования

- Node.js 14+
- npm

---

**Готово к работе!** Редактируйте `templates/` и `pages/`, затем собирайте через `npm run build`.
